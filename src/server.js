class Server {
    constructor(id) {
        this.id = id;
    }

    playSound(voiceChannel, soundName) {
        // TODO: Implement
        console.log(`This will play ${soundName}`);
    }
}

module.exports = Server;